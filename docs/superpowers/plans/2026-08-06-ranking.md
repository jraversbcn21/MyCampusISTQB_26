# Ranking global por XP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vista «Ranking» con clasificación global por XP, participación opt-in con nombre
elegido, sobre una tabla dedicada `leaderboard` en Supabase.

**Architecture:** Tabla nueva con RLS (escritura solo fila propia, lectura para
autenticados); el cliente proyecta `{display_name, xp}` en cada sync si el usuario
participa; la vista lee el top 50 + posición propia. `user_progress` intacto. Spec:
`docs/superpowers/specs/2026-08-06-ranking-design.md`.

**Tech Stack:** Vanilla JS (patrón de módulos globales del repo), supabase-js ya cargado,
arnés `scripts/verify-runtime.js` (mock DOM + mock Supabase).

## Global Constraints

- Commits directos a `master`; el hook pre-commit corre el arnés — nunca `--no-verify`.
- Toda cadena visible pasa por i18n (claves `rk_*` ES/EN pareadas); prohibido el patrón
  `i18n.lang === 'es' ? … : …`.
- Todo `display_name` ajeno que entre a `innerHTML` pasa por global `escapeHtml()`.
- Iconos: sprite `#i-*` (nunca emojis como icono de UI — gate N17).
- CSS: nada después del bloque `:focus-visible` del tail; overrides móviles del ranking
  dentro del tier `≤480px` existente; colores de estado con tokens `--*-text`.
- Botones nuevos como `<button>` reales; interactivos de template `innerHTML` con
  `role="button" tabindex="0"` (keydown delegado existente); nunca listeners por elemento.
- `js/content.js` y `js/questions.js` no se tocan.
- Checks estáticos anclados a regla/código real (`/\.selector \{/`), nunca `includes()`.

---

### Task 1: SQL de Supabase (paso manual de dashboard)

**Files:** ninguno (dashboard de Supabase). **Quién:** Jorge (o sesión con acceso al dashboard).

**Interfaces:**
- Produces: tabla `public.leaderboard` con RLS, consumida por Tasks 4–6.

- [ ] **Step 1: Ejecutar en SQL Editor del proyecto Supabase**

```sql
create table public.leaderboard (
  user_id      uuid primary key references auth.users(id) on delete cascade,
  display_name text not null check (char_length(display_name) between 1 and 30),
  xp           integer not null default 0 check (xp >= 0 and xp <= 500000),
  updated_at   timestamptz not null default now()
);

alter table public.leaderboard enable row level security;

create policy "leaderboard_insert_own" on public.leaderboard
  for insert to authenticated with check (auth.uid() = user_id);
create policy "leaderboard_update_own" on public.leaderboard
  for update to authenticated using (auth.uid() = user_id);
create policy "leaderboard_delete_own" on public.leaderboard
  for delete to authenticated using (auth.uid() = user_id);
create policy "leaderboard_select_all" on public.leaderboard
  for select to authenticated using (true);

create index leaderboard_xp_desc on public.leaderboard (xp desc);
```

- [ ] **Step 2: Verificar las políticas con el SQL real** (procedimiento RLS 2026-07-02)

```sql
select policyname, cmd, qual, with_check from pg_policies
where tablename = 'leaderboard';
```
Esperado: 4 políticas; `insert/update/delete` con `auth.uid() = user_id`; `select` con
`true` y rol `authenticated`. Confirmar también `rowsecurity = true` en `pg_tables`.

**Nota de orden:** el código cliente degrada limpio si la tabla no existe (la vista muestra
`rk_error`), pero **no desplegar a producción hasta completar esta task.**

---

### Task 2: Claves i18n `rk_*`

**Files:**
- Modify: `js/i18n.js` (bloques `es:` y `en:` de `TRANSLATIONS`)
- Test: `scripts/verify-runtime.js` (familia N27, nueva, tras el bloque N26)

**Interfaces:**
- Produces: claves `nav_ranking` + `rk_*` usadas por Tasks 3–5 (nombres exactos abajo).

- [ ] **Step 1: Escribir el check que falla** — en `verify-runtime.js`, después del cierre
del bloque N26 y antes de `/* ---- N5 + P5`:

```js
  /* ---- N27: ranking global por XP (2026-08-06) ---- */
  {
    const ctx = loadApp();
    const rkKeys = ['nav_ranking', 'rk_intro', 'rk_publish_note', 'rk_name_label',
      'rk_name_placeholder', 'rk_join', 'rk_leave', 'rk_rename', 'rk_error', 'rk_offline',
      'rk_empty', 'rk_loading', 'rk_pos_header', 'rk_name_header', 'rk_xp_header',
      'rk_your_position', 'rk_you', 'rk_name_invalid', 'rk_left_toast'];
    check('N27 i18n: claves rk_* definidas en ES y EN',
      rkKeys.every(k => ctx.TRANSLATIONS.es[k] && ctx.TRANSLATIONS.en[k]));
  }
```

- [ ] **Step 2: Verificar RED** — `node scripts/verify-runtime.js` → el check N27 falla.

- [ ] **Step 3: Añadir las claves** — en `js/i18n.js`, al final del bloque `es:` (tras las
`lp_*`) y su espejo en `en:`:

```js
    // Ranking (2026-08-06)
    nav_ranking: "Ranking",
    rk_intro: "Compárate con el resto de estudiantes del campus por XP acumulado.",
    rk_publish_note: "Participar publica únicamente el nombre que elijas y tu XP, visibles solo para usuarios registrados del campus. Puedes cambiar el nombre o salir cuando quieras; al salir, tu entrada se borra al momento.",
    rk_name_label: "Nombre a mostrar",
    rk_name_placeholder: "Tu nombre en el ranking",
    rk_join: "Participar en el ranking",
    rk_leave: "Salir del ranking",
    rk_rename: "Guardar nombre",
    rk_error: "El ranking no está disponible ahora mismo. Inténtalo más tarde.",
    rk_offline: "El ranking necesita conexión. Vuelve a intentarlo cuando estés en línea.",
    rk_empty: "Aún no hay participantes. ¡Sé el primero!",
    rk_loading: "Cargando ranking…",
    rk_pos_header: "Puesto",
    rk_name_header: "Nombre",
    rk_xp_header: "XP",
    rk_your_position: "Tu posición",
    rk_you: "Tú",
    rk_name_invalid: "El nombre debe tener entre 1 y 30 caracteres.",
    rk_left_toast: "Has salido del ranking. Tu entrada se ha borrado.",
```

```js
    // Ranking (2026-08-06)
    nav_ranking: "Ranking",
    rk_intro: "Compare yourself with the rest of the campus students by accumulated XP.",
    rk_publish_note: "Joining publishes only the name you choose and your XP, visible only to registered campus users. You can change your name or leave at any time; leaving deletes your entry immediately.",
    rk_name_label: "Display name",
    rk_name_placeholder: "Your name on the leaderboard",
    rk_join: "Join the ranking",
    rk_leave: "Leave the ranking",
    rk_rename: "Save name",
    rk_error: "The ranking is not available right now. Please try again later.",
    rk_offline: "The ranking needs a connection. Try again when you are online.",
    rk_empty: "No participants yet. Be the first!",
    rk_loading: "Loading ranking…",
    rk_pos_header: "Rank",
    rk_name_header: "Name",
    rk_xp_header: "XP",
    rk_your_position: "Your position",
    rk_you: "You",
    rk_name_invalid: "The name must be between 1 and 30 characters.",
    rk_left_toast: "You have left the ranking. Your entry has been deleted.",
```

- [ ] **Step 4: Verificar GREEN** — `node scripts/verify-runtime.js` → N27 y paridad global
(231→250/250) en verde, exit 0.

- [ ] **Step 5: Commit** — `git add js/i18n.js scripts/verify-runtime.js && git commit -m "feat(ranking): claves i18n rk_* (231 -> 250)"`

---

### Task 3: Markup — sprite, nav, vista, `navigate()`

**Files:**
- Modify: `index.html` (sprite ~línea 67, nav ~línea 289, views-container antes de `</div><!-- end views-container -->` ~línea 604)
- Modify: `js/app.js` (`navigate()` líneas 139–159)
- Test: `scripts/verify-runtime.js` (bloque N27)

**Interfaces:**
- Produces: `#view-ranking`, `#rankingContent`, nav `data-view="ranking"`, símbolo
  `#i-podium`; `navigate('ranking')` llama a `App.renderRanking()` (definido en Task 5;
  hasta entonces el if se añade igualmente y Task 5 lo satisface — ver Step 3).

- [ ] **Step 1: Checks que fallan** — añadir dentro del bloque N27:

```js
    const htmlSrc = fs.readFileSync(path.join(ROOT, 'index.html'), 'utf8');
    const appSrc = fs.readFileSync(path.join(ROOT, 'js', 'app.js'), 'utf8');
    check('N27 markup: símbolo #i-podium en el sprite',
      /<symbol id="i-podium" viewBox="0 0 24 24">/.test(htmlSrc));
    check('N27 markup: nav-item data-view="ranking" con icono sprite y data-i18n',
      /data-view="ranking"[\s\S]{0,200}#i-podium[\s\S]{0,200}data-i18n="nav_ranking"/.test(htmlSrc));
    check('N27 markup: vista view-ranking con #rankingContent dentro',
      /<div class="view" id="view-ranking">[\s\S]{0,400}id="rankingContent"/.test(htmlSrc));
    check('N27 nav: navigate() despacha renderRanking y titleMap tiene ranking',
      /if \(view === 'ranking'\) this\.renderRanking\(\);/.test(appSrc)
      && /ranking: 'nav_ranking'/.test(appSrc));
```

- [ ] **Step 2: Verificar RED** — los 4 checks fallan.

- [ ] **Step 3: Implementar.** En el sprite (tras `#i-coffee`, línea ~67):

```html
    <symbol id="i-podium" viewBox="0 0 24 24"><path d="M9 21V9h6v12"/><path d="M3 21v-6h6"/><path d="M15 12h6v9"/><path d="M3 21h18"/></symbol>
```

En el nav, tras el item de achievements (línea ~289):

```html
      <a href="#" class="nav-item" data-view="ranking">
        <span class="nav-icon"><svg class="icon" aria-hidden="true"><use href="#i-podium"/></svg></span>
        <span class="nav-label" data-i18n="nav_ranking">Ranking</span>
      </a>
```

En el views-container, tras `view-achievements` (línea ~603):

```html
      <div class="view" id="view-ranking">
        <p class="ranking-intro" data-i18n="rk_intro">Compárate con el resto de estudiantes del campus por XP acumulado.</p>
        <div id="rankingContent"></div>
      </div>
```

En `js/app.js` `navigate()`: añadir `ranking: 'nav_ranking'` al `titleMap` (tras
`achievements: 'nav_achievements'`) y, tras la línea
`if (view === 'achievements') this.renderAchievements();`:

```js
    if (view === 'ranking') this.renderRanking();
```

Y un stub temporal para no romper la navegación hasta Task 5 — junto a los render:
`renderRanking() {},` (Task 5 lo reemplaza).

- [ ] **Step 4: Verificar GREEN** — arnés completo exit 0 (el check i18n
«usadas-pero-no-definidas» pasa porque `nav_ranking`/`rk_intro` existen desde Task 2).

- [ ] **Step 5: Commit** — `git add index.html js/app.js scripts/verify-runtime.js && git commit -m "feat(ranking): nav, vista y sprite #i-podium"`

---

### Task 4: Mock extendido + estado + acciones de datos

**Files:**
- Modify: `scripts/verify-runtime.js` (`makeSupabaseMock`, líneas 74–94, y bloque N27)
- Modify: `js/app.js` (nuevos métodos junto a la sección de render del ranking)

**Interfaces:**
- Consumes: `supabaseClient` global, `Auth.user.id`, `App.state`, `App.saveState()`.
- Produces: `App._rankingEnsureState()`, `App._rankingUpsertSelf(name)` →
  `Promise<boolean>`, `App.rankingJoin(name)`, `App.rankingRename(name)`,
  `App.rankingLeave()`, `App._rankingFetch()` → `Promise<{rows, total, myPos}>`.
  Mock: `calls.upserts` ahora lleva `{table, ...fila}`; nuevos `calls.deletes`
  `[{table}]`; `opts.selectQueue` (array de resultados consumidos en orden por los
  `await` de selects encadenados).

- [ ] **Step 1: Extender el mock** (compatible hacia atrás — los checks existentes leen
`calls.upserts` y sus campos de fila, que se conservan):

```js
function makeSupabaseMock(opts = {}) {
  const calls = { upserts: [], deletes: [] };
  const client = {
    _calls: calls,
    auth: {
      onAuthStateChange(cb) { calls.authStateCb = cb; return { data: { subscription: {} } }; },
      getSession: async () => ({ data: { session: 'session' in opts ? opts.session : { access_token: 'tok-mock' } } }),
      signOut: async () => ({ error: null }),
    },
    from(table) {
      const chain = {
        select() { return chain; },
        order() { return chain; },
        limit() { return chain; },
        gt() { return chain; },
        eq() { return chain; },
        single: async () => opts.singleResult || { data: null, error: { code: 'PGRST116' } },
        upsert: async (row) => { calls.upserts.push({ table, ...row }); return { error: null }; },
        delete() { calls.deletes.push({ table }); return { eq: async () => ({ error: null }) }; },
        // Los selects de lista/count se esperan directamente (thenable): consumen
        // opts.selectQueue en orden, o un resultado vacío por defecto.
        then(resolve) {
          resolve((opts.selectQueue && opts.selectQueue.length)
            ? opts.selectQueue.shift()
            : { data: [], count: 0, error: null });
        },
      };
      return chain;
    },
  };
  return client;
}
```

- [ ] **Step 2: Checks que fallan** — dentro del bloque N27:

```js
    // Comportamental: opt-in / opt-out / fetch contra el mock.
    {
      const ctx = loadApp();
      ctx.App.state = ctx.App.loadState();
      ctx.App.state.xp = 777;
      ctx.window.CAMPUS_USER_ID = 'u-rk';
      // Auth.user es lo que leen las acciones:
      ctx.Auth.user = { id: 'u-rk', email: 'a@b.c', user_metadata: {} };
      let saved = 0; ctx.App.saveState = () => { saved++; };
      ctx.App.renderRanking = () => {}; // Task 5; aquí solo datos
      ctx.App.showToast = () => {};

      check('N27 estado: _rankingEnsureState migra estados legados (defaults false/\'\')',
        (ctx.App._rankingEnsureState(), ctx.App.state.rankingOptIn === false
          && ctx.App.state.rankingName === ''));

      // Los awaits van directos: el cuerpo principal del arnés es async
      // (mismo patrón que los checks N25 con _onAuthSuccess).
      await ctx.App.rankingJoin('  Jorge C  ');
      const up = ctx.supabase._calls.upserts.find(u => u.table === 'leaderboard');
      check('N27 join: upsert a leaderboard con nombre limpio y xp del estado',
        up && up.display_name === 'Jorge C' && up.xp === 777 && up.user_id === 'u-rk');
      check('N27 join: activa rankingOptIn, guarda rankingName y persiste',
        ctx.App.state.rankingOptIn === true && ctx.App.state.rankingName === 'Jorge C'
        && saved > 0);

      const before = ctx.supabase._calls.upserts.length;
      await ctx.App.rankingJoin('');
      check('N27 join: nombre vacío → rechazado sin tocar Supabase',
        ctx.supabase._calls.upserts.length === before);
      await ctx.App.rankingJoin('x'.repeat(31));
      check('N27 join: nombre de 31 chars → rechazado',
        ctx.supabase._calls.upserts.length === before);

      await ctx.App.rankingLeave();
      check('N27 leave: DELETE en leaderboard y flag a false (el nombre se conserva)',
        ctx.supabase._calls.deletes.some(d => d.table === 'leaderboard')
        && ctx.App.state.rankingOptIn === false && ctx.App.state.rankingName === 'Jorge C');
```

Y el fetch (contexto propio con `selectQueue`):

```js
    {
      const sb = makeSupabaseMock({ selectQueue: [
        { data: [{ user_id: 'a', display_name: 'Ana', xp: 900 }], count: 60, error: null },
        { count: 7, error: null },
      ] });
      const ctx = loadApp({ supabase: sb });
      ctx.App.state = ctx.App.loadState();
      ctx.App.state.xp = 100; ctx.App.state.rankingOptIn = true; ctx.App.state.rankingName = 'Yo';
      ctx.Auth.user = { id: 'u-rk', email: 'a@b.c', user_metadata: {} };
      const res = await ctx.App._rankingFetch();
      check('N27 fetch: top + total + posición propia (superiores + 1) cuando estoy fuera del top',
        res.rows.length === 1 && res.total === 60 && res.myPos === 8);
    }
```

- [ ] **Step 3: Verificar RED** — todos los checks nuevos fallan (métodos inexistentes).

- [ ] **Step 4: Implementar en `js/app.js`** (sección nueva `/* ===== RANKING ===== */`
antes de la sección TTS):

```js
  /* ===== RANKING (2026-08-06) ===== */
  // Guard de migración en el punto de uso (lección celebratedChapters): estados
  // legados y copias de nube sin los campos nuevos.
  _rankingEnsureState() {
    if (typeof this.state.rankingOptIn !== 'boolean') this.state.rankingOptIn = false;
    if (typeof this.state.rankingName !== 'string') this.state.rankingName = '';
  },

  async _rankingUpsertSelf(name) {
    const clean = String(name || '').trim();
    if (clean.length < 1 || clean.length > 30) {
      this.showToast(i18n.t('rk_name_invalid'), 'warning');
      return false;
    }
    if (typeof supabaseClient === 'undefined' || !supabaseClient
        || typeof Auth === 'undefined' || !Auth.user) {
      this.showToast(i18n.t('rk_offline'), 'warning');
      return false;
    }
    const { error } = await supabaseClient.from('leaderboard').upsert(
      { user_id: Auth.user.id, display_name: clean, xp: this.state.xp,
        updated_at: new Date().toISOString() },
      { onConflict: 'user_id' }
    );
    if (error) { this.showToast(i18n.t('rk_error'), 'error'); return false; }
    this.state.rankingName = clean;
    return true;
  },

  async rankingJoin(name) {
    this._rankingEnsureState();
    const ok = await this._rankingUpsertSelf(name);
    if (!ok) return false;
    this.state.rankingOptIn = true;
    this.saveState();
    this.renderRanking();
    return true;
  },

  async rankingRename(name) {
    this._rankingEnsureState();
    const ok = await this._rankingUpsertSelf(name);
    if (!ok) return false;
    this.saveState();
    this.renderRanking();
    return true;
  },

  async rankingLeave() {
    this._rankingEnsureState();
    if (typeof supabaseClient === 'undefined' || !supabaseClient
        || typeof Auth === 'undefined' || !Auth.user) {
      this.showToast(i18n.t('rk_offline'), 'warning');
      return false;
    }
    const { error } = await supabaseClient.from('leaderboard')
      .delete().eq('user_id', Auth.user.id);
    if (error) { this.showToast(i18n.t('rk_error'), 'error'); return false; }
    this.state.rankingOptIn = false; // rankingName se conserva por si vuelve
    this.saveState();
    this.showToast(i18n.t('rk_left_toast'), 'success');
    this.renderRanking();
    return true;
  },

  // Top 50 + total + posición propia. Lanza en error de red: el caller decide.
  async _rankingFetch() {
    const { data, count, error } = await supabaseClient
      .from('leaderboard')
      .select('user_id, display_name, xp', { count: 'exact' })
      .order('xp', { ascending: false })
      .limit(50);
    if (error) throw error;
    const rows = data || [];
    let myPos = null;
    const uid = (typeof Auth !== 'undefined' && Auth.user) ? Auth.user.id : null;
    if (this.state.rankingOptIn && uid && !rows.some(r => r.user_id === uid)) {
      const { count: above, error: e2 } = await supabaseClient
        .from('leaderboard')
        .select('user_id', { count: 'exact', head: true })
        .gt('xp', this.state.xp);
      if (!e2 && typeof above === 'number') myPos = above + 1;
    }
    return { rows, total: count || 0, myPos };
  },
```

- [ ] **Step 5: Verificar GREEN** — arnés completo exit 0 (incluidos N1/N2/N22, que usan
el mock extendido).

- [ ] **Step 6: Commit** — `git add js/app.js scripts/verify-runtime.js && git commit -m "feat(ranking): estado, acciones join/leave/rename y fetch del top"`

---

### Task 5: `renderRanking()` — vista, XSS, degradación

**Files:**
- Modify: `js/app.js` (reemplaza el stub `renderRanking() {}` de Task 3)
- Test: `scripts/verify-runtime.js` (bloque N27)

**Interfaces:**
- Consumes: `_rankingFetch()`, `rankingJoin/rankingLeave/rankingRename`,
  global `escapeHtml()`, `App._getDisplayName()`, claves `rk_*`.
- Produces: `App.renderRanking()` (async), pinta en `#rankingContent`.

- [ ] **Step 1: Checks que fallan:**

```js
    // XSS: nombres ajenos escapados; render de tabla y panel opt-in.
    {
      const sb = makeSupabaseMock({ selectQueue: [
        { data: [
          { user_id: 'evil', display_name: '<img src=x onerror=alert(1)>', xp: 999 },
          { user_id: 'u-rk', display_name: 'Yo', xp: 777 },
        ], count: 2, error: null },
      ] });
      const ctx = loadApp({ supabase: sb });
      ctx.App.state = ctx.App.loadState();
      ctx.App.state.xp = 777; ctx.App.state.rankingOptIn = true; ctx.App.state.rankingName = 'Yo';
      ctx.Auth.user = { id: 'u-rk', email: 'a@b.c', user_metadata: {} };
      await ctx.App.renderRanking();
      const html = ctx.document.getElementById('rankingContent').innerHTML;
      check('N27 xss: display_name ajeno escapado en el innerHTML',
        !html.includes('<img src=x') && html.includes('&lt;img'));
      check('N27 render: fila propia resaltada (clase rk-me) y botón de salir presente',
        html.includes('rk-me') && html.includes('App.rankingLeave()'));
    }
    {
      const ctx = loadApp();
      ctx.App.state = ctx.App.loadState();
      ctx.Auth.user = { id: 'u-rk', email: 'a@b.c', user_metadata: {} };
      await ctx.App.renderRanking();
      const html = ctx.document.getElementById('rankingContent').innerHTML;
      check('N27 render: sin participar → panel de opt-in con nota de privacidad y botón Participar',
        html.includes('App.rankingJoin') && html.includes('rk-publish-note')
        && html.includes('id="rkNameInput"'));
    }
```

Nota arnés: `document.getElementById` mockeado crea el elemento al vuelo — el `innerHTML`
escrito por `renderRanking` es legible después, mismo patrón que los checks N4.

- [ ] **Step 2: Verificar RED.**

- [ ] **Step 3: Implementar** (reemplaza el stub):

```js
  async renderRanking() {
    this._rankingEnsureState();
    const cont = document.getElementById('rankingContent');
    if (!cont) return;
    if (typeof supabaseClient === 'undefined' || !supabaseClient) {
      cont.innerHTML = `<p class="ranking-msg">${i18n.t('rk_offline')}</p>`;
      return;
    }
    cont.innerHTML = `<p class="ranking-msg">${i18n.t('rk_loading')}</p>`;
    let res;
    try { res = await this._rankingFetch(); }
    catch (e) {
      cont.innerHTML = `<p class="ranking-msg">${i18n.t('rk_error')}</p>`;
      return;
    }
    const uid = (typeof Auth !== 'undefined' && Auth.user) ? Auth.user.id : null;
    let html = '';

    if (!this.state.rankingOptIn) {
      const suggested = escapeHtml(this.state.rankingName || this._getDisplayName());
      html += `
        <div class="ranking-optin">
          <p class="rk-publish-note">${i18n.t('rk_publish_note')}</p>
          <label class="rk-name-label" for="rkNameInput">${i18n.t('rk_name_label')}</label>
          <input type="text" id="rkNameInput" maxlength="30" value="${suggested}"
                 placeholder="${i18n.t('rk_name_placeholder')}">
          <button class="btn-primary rk-join-btn"
                  onclick="App.rankingJoin(document.getElementById('rkNameInput').value)">
            ${i18n.t('rk_join')}</button>
        </div>`;
    } else {
      const own = escapeHtml(this.state.rankingName);
      html += `
        <div class="ranking-controls">
          <input type="text" id="rkNameInput" maxlength="30" value="${own}"
                 aria-label="${i18n.t('rk_name_label')}">
          <button class="rk-rename-btn"
                  onclick="App.rankingRename(document.getElementById('rkNameInput').value)">
            ${i18n.t('rk_rename')}</button>
          <button class="rk-leave-btn" onclick="App.rankingLeave()">${i18n.t('rk_leave')}</button>
        </div>`;
    }

    if (!res.rows.length) {
      html += `<p class="ranking-msg">${i18n.t('rk_empty')}</p>`;
    } else {
      const body = res.rows.map((r, i) => {
        const me = uid && r.user_id === uid;
        return `<tr${me ? ' class="rk-me"' : ''}>
          <td class="rk-pos">${i + 1}</td>
          <td class="rk-name">${escapeHtml(r.display_name)}${me ? ` <span class="rk-you-tag">${i18n.t('rk_you')}</span>` : ''}</td>
          <td class="rk-xp">${Number(r.xp) || 0}</td>
        </tr>`;
      }).join('');
      html += `
        <div class="table-scroll"><table class="ranking-table">
          <thead><tr>
            <th scope="col">${i18n.t('rk_pos_header')}</th>
            <th scope="col">${i18n.t('rk_name_header')}</th>
            <th scope="col">${i18n.t('rk_xp_header')}</th>
          </tr></thead>
          <tbody>${body}</tbody>
        </table></div>`;
      if (res.myPos !== null) {
        html += `<p class="ranking-mypos">${i18n.t('rk_your_position')}: #${res.myPos} / ${res.total}</p>`;
      }
    }
    cont.innerHTML = html;
  },
```

(`xp` ajeno pasa por `Number() || 0` — también es dato de otra fila; nunca interpolar el
valor crudo.)

- [ ] **Step 4: Verificar GREEN** — arnés completo exit 0.

- [ ] **Step 5: Commit** — `git add js/app.js scripts/verify-runtime.js && git commit -m "feat(ranking): renderRanking con opt-in, tabla escapada y degradacion"`

---

### Task 6: Upsert del ranking en el ciclo de sync

**Files:**
- Modify: `js/sync.js` (`_push`, líneas 119–132)
- Test: `scripts/verify-runtime.js` (bloque N27)

**Interfaces:**
- Consumes: `state.rankingOptIn`/`rankingName`/`xp` (el `state` que `_push` ya recibe).
- Produces: `Sync._pushRanking(userId, state)`; llamada condicionada dentro de `_push`.

- [ ] **Step 1: Checks que fallan:**

```js
    // Sync: upsert del ranking tras el push del progreso, gated por el flag,
    // y nunca bloqueante.
    {
      const ctx = loadApp();
      ctx.Sync._reconciled = true;
      const st = { xp: 500, rankingOptIn: true, rankingName: 'Yo', _updatedAt: 1 };
      await ctx.Sync._push('u-rk', st);
      const tables = ctx.supabase._calls.upserts.map(u => u.table);
      check('N27 sync: con optIn el push sube progreso Y ranking (en ese orden)',
        tables.indexOf('user_progress') !== -1
        && tables.indexOf('leaderboard') > tables.indexOf('user_progress'));

      const before = ctx.supabase._calls.upserts.filter(u => u.table === 'leaderboard').length;
      await ctx.Sync._push('u-rk', { xp: 500, rankingOptIn: false, _updatedAt: 2 });
      check('N27 sync: sin optIn el push no toca leaderboard',
        ctx.supabase._calls.upserts.filter(u => u.table === 'leaderboard').length === before);
    }
    {
      // Un fallo del upsert del ranking no rompe el push del progreso.
      const sb = makeSupabaseMock();
      const origFrom = sb.from.bind(sb);
      sb.from = (table) => {
        if (table === 'leaderboard') return { upsert: async () => { throw new Error('boom'); } };
        return origFrom(table);
      };
      const ctx = loadApp({ supabase: sb });
      ctx.Sync._reconciled = true;
      let threw = false;
      try { await ctx.Sync._push('u-rk', { xp: 1, rankingOptIn: true, rankingName: 'Yo', _updatedAt: 3 }); }
      catch (e) { threw = true; }
      check('N27 sync: el upsert del ranking reventando no propaga ni impide el push',
        !threw && sb._calls.upserts.some(u => u.table === 'user_progress'));
    }
```

- [ ] **Step 2: Verificar RED.**

- [ ] **Step 3: Implementar en `js/sync.js`.** En `_push`, tras `this._dirty = false;`:

```js
      // Ranking (2026-08-06): proyección {nombre, xp} si el usuario participa.
      // Posterior e independiente: su fallo jamás bloquea el progreso.
      if (state && state.rankingOptIn) await this._pushRanking(userId, state);
```

Y el método nuevo, tras `_push`:

```js
  // Upsert de la fila propia en leaderboard. Nunca lanza. El flush keepalive de
  // visibilitychange NO lo replica (deliberado, spec): el siguiente sync con
  // sesión abierta pone la tabla al día.
  async _pushRanking(userId, state) {
    try {
      const { error } = await supabaseClient
        .from('leaderboard')
        .upsert(
          { user_id: userId, display_name: state.rankingName, xp: state.xp,
            updated_at: new Date().toISOString() },
          { onConflict: 'user_id' }
        );
      if (error) throw error;
    } catch (e) {
      console.warn('[Sync] push del ranking falló (no bloquea el progreso):', e.message);
    }
  },
```

- [ ] **Step 4: Verificar GREEN** — arnés completo exit 0 (N1/N2/N22 intactos: sus estados
no llevan `rankingOptIn`).

- [ ] **Step 5: Commit** — `git add js/sync.js scripts/verify-runtime.js && git commit -m "feat(ranking): upsert de la proyeccion nombre+xp en el ciclo de sync"`

---

### Task 7: CSS + gate responsive

**Files:**
- Modify: `css/styles.css` (sección nueva tras el bloque del celebration-modal, ~línea
  2460, ANTES de la sección de onboarding; overrides móviles dentro del tier `≤480px`)
- Modify: `scripts/validate-responsive.js` (línea 46: añadir `'ranking'` a `VIEWS`)
- Test: `scripts/verify-runtime.js` (bloque N27) + `node scripts/validate-responsive.js`

**Interfaces:**
- Consumes: clases emitidas por Task 5 (`.ranking-optin`, `.ranking-controls`,
  `.ranking-table`, `.rk-me`, `.rk-you-tag`, `.ranking-msg`, `.ranking-mypos`,
  `.rk-join-btn`, `.rk-rename-btn`, `.rk-leave-btn`, `.rk-pos`, `.rk-name`, `.rk-xp`).

- [ ] **Step 1: Checks que fallan:**

```js
    const cssSrc = fs.readFileSync(path.join(ROOT, 'css', 'styles.css'), 'utf8');
    check('N27 css: .ranking-table definida antes del bloque reduced-motion (orden del tail)',
      /\.ranking-table \{/.test(cssSrc)
      && cssSrc.search(/\.ranking-table \{/) < cssSrc.search(/@media \(prefers-reduced-motion: reduce\)/));
    check('N27 css: fila propia resaltada y botones táctiles de ≥44px',
      /\.rk-me \{/.test(cssSrc) && /\.rk-join-btn/.test(cssSrc));
    check('N27 responsive: la vista ranking entra en el barrido del gate',
      /'achievements', 'ranking'/.test(fs.readFileSync(path.join(ROOT, 'scripts', 'validate-responsive.js'), 'utf8')));
```

- [ ] **Step 2: Verificar RED.**

- [ ] **Step 3: Implementar.** Sección nueva (tras el CSS del celebration-modal, antes de
ONBOARDING TOUR):

```css
/* ===================================================
   RANKING (2026-08-06)
   Tabla del top + panel de opt-in. Nombres ajenos truncan con ellipsis; la
   tabla vive en .table-scroll (patrón lecciones móvil). Botones a 44px min.
   =================================================== */
.ranking-intro { color: var(--text2); margin-bottom: 16px; }
.ranking-msg { color: var(--text2); padding: 24px 0; }
.ranking-optin, .ranking-controls {
  background: var(--bg2);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 16px;
  margin-bottom: 20px;
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
}
.ranking-optin { flex-direction: column; align-items: stretch; }
.rk-publish-note { color: var(--text2); font-size: 0.85rem; }
.rk-name-label { font-size: 0.8rem; color: var(--text2); }
#rkNameInput {
  background: var(--bg3);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  color: var(--text);
  padding: 10px 12px;
  font-size: 1rem; /* 16px: mata el auto-zoom de iOS (gate N13) */
  min-width: 0;
  flex: 1;
}
.rk-join-btn, .rk-rename-btn, .rk-leave-btn {
  min-height: 44px;
  padding: 10px 16px;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  background: var(--bg3);
  color: var(--text);
  cursor: pointer;
  font-size: 0.9rem;
}
.rk-join-btn {
  background: var(--primary-dark); /* blanco sobre --primary falla AA (regla .bmc-fab) */
  color: #fff;
  border: none;
}
.rk-leave-btn { color: var(--danger-text); }
.ranking-table { width: 100%; border-collapse: collapse; }
.ranking-table th, .ranking-table td {
  text-align: left;
  padding: 10px 12px;
  border-bottom: 1px solid var(--border);
}
.ranking-table th { color: var(--text3); font-size: 0.75rem; text-transform: uppercase; }
.rk-pos { width: 56px; color: var(--text2); }
.rk-xp { text-align: right; font-weight: 600; }
.rk-name { overflow-wrap: break-word; }
.rk-me { background: rgba(108,99,255,0.1); }
.rk-me .rk-name { color: var(--primary-text); font-weight: 600; }
.rk-you-tag { font-size: 0.7rem; color: var(--primary-text); font-weight: 700; }
.ranking-mypos { margin-top: 12px; color: var(--text); font-weight: 600; }
```

En el tier `≤480px` existente (dentro del bloque, junto a las demás reglas):

```css
  .ranking-optin, .ranking-controls { padding: 12px; }
  .ranking-table th, .ranking-table td { padding: 8px; }
```

En `scripts/validate-responsive.js` línea 46:

```js
const VIEWS = ['dashboard', 'curriculum', 'flashcards', 'simulator', 'glossary', 'progress', 'achievements', 'ranking'];
```

- [ ] **Step 4: Verificar GREEN** — `node scripts/verify-runtime.js` exit 0,
`node scripts/validate-contrast.js` exit 0 (solo se usan tokens existentes) y
`node scripts/validate-responsive.js` exit 0 (la vista nueva sin overflow a
320/375/414/600; en el gate, sin red externa, muestra el mensaje `rk_offline` — también
medible).

- [ ] **Step 5: Commit** — `git add css/styles.css scripts/validate-responsive.js scripts/verify-runtime.js && git commit -m "feat(ranking): estilos de la vista y barrido responsive"`

---

### Task 8: privacy.html (mismo despliegue — regla del repo)

**Files:**
- Modify: `privacy.html` (secciones ES y EN de datos/servicios + fecha «última actualización»)

- [ ] **Step 1: Añadir en la sección de datos ES** (tras el bloque que describe el
progreso sincronizado), replicando el patrón bilingüe existente del archivo:

```html
<h3>Ranking voluntario</h3>
<p>Si decides participar en el ranking del campus (función opcional, desactivada por
defecto), se publica únicamente el nombre que tú elijas y tu XP acumulado, visibles solo
para usuarios registrados de la aplicación. Puedes cambiar ese nombre o salir del ranking
en cualquier momento desde la propia vista; al salir, tu entrada se elimina de inmediato
de nuestra base de datos (Supabase, región UE).</p>
```

Y su espejo EN:

```html
<h3>Voluntary ranking</h3>
<p>If you choose to join the campus ranking (an optional feature, off by default), only
the name you pick and your accumulated XP are published, visible solely to registered
users of the application. You can change that name or leave the ranking at any time from
the ranking view itself; leaving deletes your entry immediately from our database
(Supabase, EU region).</p>
```

- [ ] **Step 2: Bump de la fecha** «última actualización» ES/EN a la fecha del despliegue.

- [ ] **Step 3: Verificación** — abrir `privacy.html` servida en local y comprobar ambas
lenguas con el switcher propio de la página.

- [ ] **Step 4: Commit** — `git add privacy.html && git commit -m "docs(privacy): seccion del ranking voluntario ES/EN"`

---

### Task 9: CLAUDE.md + verificación final

**Files:**
- Modify: `CLAUDE.md` (sección nueva al final: enfoque, SQL/RLS aplicado y verificado,
  flujos, hazards de edición — mock extendido thenable, upsert condicionado en `_push`,
  escapeHtml obligatorio en nombres ajenos, el flush keepalive no replica el ranking —
  y la familia N27)

- [ ] **Step 1: Redactar la sección** siguiendo el formato de las existentes (título con
fecha, decisiones load-bearing, gate al final). Incluir la nota de que `#i-podium` no está
protegido por N17 (lista hardcodeada de 26) sino por N27, como pasó con `#i-coffee`/N19.

- [ ] **Step 2: Verificación completa** —

```bash
node scripts/verify-runtime.js
node scripts/validate-contrast.js
node scripts/validate-responsive.js
node scripts/validate-content.js
node scripts/validate-questions.js
```
Todos exit 0.

- [ ] **Step 3: Commit** — `git add CLAUDE.md && git commit -m "docs: sincroniza CLAUDE.md — ranking global por XP (N27)"`

- [ ] **Step 4: Push + deploy** — `git push origin master` y, **solo con Task 1 aplicada
en Supabase**, `vercel deploy --prod --yes` (con `NODE_EXTRA_CA_CERTS` si red corporativa).
Verificar en producción que la vista carga y el opt-in funciona end-to-end con un usuario
real; probar también el opt-out (la fila desaparece en el dashboard de Supabase).
